@{
    ViewData["Title"] = "Page Templates";
    var pageId = ViewBag.PageId;
    var position = ViewBag.Position ?? "bottom";
}

<h2 class="mb-4 mt-2 ps-1">Selecteer een alinea template</h2>

<div class="container">
    <div class="row">
        <div class="col-12 mb-4">
            <div class="template border p-3 position-relative">
                <div class="position-absolute top-0 end-0 m-2">
                    <form method="post" action="@Url.Action("AddContent", "Home")" style="display: inline;">
                        @Html.AntiForgeryToken()
                        <input type="hidden" name="pageId" value="@pageId" />
                        <input type="hidden" name="type" value="alinea#1" />
                        <input type="hidden" name="position" value="@position" />
                        <button type="submit" class="btn btn-sm btn-success">Alinea toevoegen</button>
                    </form>
                </div>
                <h4>Alinea #1</h4>
                <div class="row">
                    <div class="col-12 mb-3"><div class="border p-2 bg-light" style="min-height: 40px;">Hier wordt de titel van de alinea weergegeven</div></div>
                    <div class="col-12"><div class="border p-2 bg-light" style="height: 160px;">Hier wordt in tekst van de alinea weergegeven. Deze alinea is schermvullend</div></div>
                </div>
            </div>
        </div>
        <div class="col-12 mb-4">
            <div class="template border p-3 position-relative">
                <div class="position-absolute top-0 end-0 m-2">
                    <form method="post" action="@Url.Action("AddContent", "Home")" style="display: inline;">
                        @Html.AntiForgeryToken()
                        <input type="hidden" name="pageId" value="@pageId" />
                        <input type="hidden" name="type" value="alinea#2" />
                        <input type="hidden" name="position" value="@position" />
                        <button type="submit" class="btn btn-sm btn-success">Alinea toevoegen</button>
                    </form>
                </div>
                <h4>Alinea #2</h4>
                <div class="mb-3"><div class="border p-2 bg-light" style="min-height: 40px;">Hier wordt de titel van de alinea weergegeven</div></div>
                <div class="alinea-image-right">
                    <div class="text-content"><div class="border p-2 bg-light" style="min-height: 160px;">Hier wordt in tekst van de alinea weergegeven met aan de rechterkant van de tekst een afbeeldingen slideshow.</div></div>
                    <div class="image-wrapper"><img src="/images/placeholder.png" class="border" style="width: 100%; height: auto; display: block; border-radius: 8px;" alt="Placeholder" /></div>
                </div>
            </div>
        </div>
        <div class="col-12 mb-4">
            <div class="template border p-3 position-relative">
                <div class="position-absolute top-0 end-0 m-2">
                    <form method="post" action="@Url.Action("AddContent", "Home")" style="display: inline;">
                        @Html.AntiForgeryToken()
                        <input type="hidden" name="pageId" value="@pageId" />
                        <input type="hidden" name="type" value="alinea#3" />
                        <input type="hidden" name="position" value="@position" />
                        <button type="submit" class="btn btn-sm btn-success">Alinea toevoegen</button>
                    </form>
                </div>
                <h4>Alinea #3</h4>
                <div class="mb-3"><div class="border p-2 bg-light" style="min-height: 40px;">Hier wordt de titel van de alinea weergegeven</div></div>
                <div class="alinea-image-left">
                    <div class="image-wrapper"><img src="/images/placeholder.png" class="border" style="width: 100%; height: auto; display: block; border-radius: 8px;" alt="Placeholder" /></div>
                    <div class="text-content"><div class="border p-2 bg-light" style="min-height: 160px;">Hier wordt in tekst van de alinea weergegeven met aan de linkerkant van de tekst een afbeeldingen slideshow.</div></div>
                </div>
            </div>
        </div>
        <div class="col-12 mb-4">
            <div class="template border p-3 position-relative">
                <div class="position-absolute top-0 end-0 m-2">
                    <form method="post" action="@Url.Action("AddContent", "Home")" style="display: inline;">
                        @Html.AntiForgeryToken()
                        <input type="hidden" name="pageId" value="@pageId" />
                        <input type="hidden" name="type" value="alinea#4" />
                        <input type="hidden" name="position" value="@position" />
                        <button type="submit" class="btn btn-sm btn-success">Alinea toevoegen</button>
                    </form>
                </div>
                <h4>Alinea #4 (zonder titel)</h4>
                <div class="row">
                    <div class="col-12"><div class="border p-2 bg-light" style="height: 160px;">Hier wordt in tekst van de alinea weergegeven. Deze alinea is schermvullend</div></div>
                </div>
            </div>
        </div>
        <div class="col-12 mb-4">
            <div class="template border p-3 position-relative">
                <div class="position-absolute top-0 end-0 m-2">
                    <form method="post" action="@Url.Action("AddContent", "Home")" style="display: inline;">
                        @Html.AntiForgeryToken()
                        <input type="hidden" name="pageId" value="@pageId" />
                        <input type="hidden" name="type" value="alinea#5" />
                        <input type="hidden" name="position" value="@position" />
                        <button type="submit" class="btn btn-sm btn-success">Alinea toevoegen</button>
                    </form>
                </div>
                <h4>Alinea #5 (zonder titel)</h4>
                <div class="alinea-image-right">
                    <div class="text-content"><div class="border p-2 bg-light" style="min-height: 160px;">Hier wordt in tekst van de alinea weergegeven met aan de rechterkant van de tekst een afbeeldingen slideshow.</div></div>
                    <div class="image-wrapper"><img src="/images/placeholder.png" class="border" style="width: 100%; height: auto; display: block; border-radius: 8px;" alt="Placeholder" /></div>
                </div>
            </div>
        </div>
        <div class="col-12 mb-4">
            <div class="template border p-3 position-relative">
                <div class="position-absolute top-0 end-0 m-2">
                    <form method="post" action="@Url.Action("AddContent", "Home")" style="display: inline;">
                        @Html.AntiForgeryToken()
                        <input type="hidden" name="pageId" value="@pageId" />
                        <input type="hidden" name="type" value="alinea#6" />
                        <input type="hidden" name="position" value="@position" />
                        <button type="submit" class="btn btn-sm btn-success">Alinea toevoegen</button>
                    </form>
                </div>
                <h4>Alinea #6 (zonder titel)</h4>
                <div class="alinea-image-left">
                    <div class="image-wrapper"><img src="/images/placeholder.png" class="border" style="width: 100%; height: auto; display: block; border-radius: 8px;" alt="Placeholder" /></div>
                    <div class="text-content"><div class="border p-2 bg-light" style="min-height: 160px;">Hier wordt in tekst van de alinea weergegeven met aan de linkerkant van de tekst een afbeeldingen slideshow.</div></div>
                </div>
            </div>
        </div>
    </div>
    <div style="height: 80px;"></div>
</div>